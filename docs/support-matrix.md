# Support Matrix

Official support is intentionally narrow:

- macOS with Homebrew
- Windows with WinGet
- Ubuntu/Debian with `apt-get`

Everything else is best effort. This keeps backend install/update guidance concrete and testable.

