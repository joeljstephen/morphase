{
  "name": "@morphase/engine",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@morphase/plugin-sdk": "workspace:*",
    "@morphase/plugins": "workspace:*",
    "@morphase/shared": "workspace:*",
    "execa": "^9.6.0"
  }
}

